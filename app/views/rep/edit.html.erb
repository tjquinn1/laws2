
<h1>Editing article</h1>

<%= form_for :rep, url: article_path(@article), method: :patch do |f| %>

    <% if @article.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@rep.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @rep.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :firstName %><br>
      <%= f.text_field :firstName %>
    </p>

    <p>
      <%= f.label :lastName %><br>
      <%= f.text_area :lastName %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>

<%= link_to 'Back', reps_path %>