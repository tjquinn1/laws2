

<%= form_for :rep, url: reps_path do |f| %>
    <% if @reps.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@reps.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @reps.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :lastName %><br>
      <%= f.text_field :lastName %>
    </p>

    <p>
      <%= f.label :firstName %><br>
      <%= f.text_area :firstName %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>



<%= link_to 'Back', reps_path %>